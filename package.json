{
  "name": "jarvis-ipad",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=22"
  },
  "description": "Local-first iPad workspace with personal memory, voice utilities and optional local AI",
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.1.1"
  }
}